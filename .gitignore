# Dependencies
node_modules/
.pnp
.pnp.js

# Environment
.env
.env.local
.env.production
.env.*.local

# Los mismos ficheros SIN punto delante. Existen en backend/ y los patrones de
# arriba no los cogen: `.env.production` no casa con `env.production`. Estaban
# sin seguir y a un `git add -A` de subirse con sus credenciales dentro.
env
env.*
!env.example


# Build
dist/
build/
*.tgz

# Logs
logs/
*.log
npm-debug.log*
pm2-*.log

# OS
.DS_Store
Thumbs.db
desktop.ini

# IDE
.vscode/
.idea/
*.swp
*.swo
*~

# Test
coverage/
playwright-report/
test-results/
.playwright/

# Uploads
uploads/

# Claude Code
.claude/

# Temp
*.tmp
*.bak

# Screenshots de validacion (transitorios, no commiteables)
/*.png
/*.jpeg
.playwright-mcp/
frontend/audit/

# Scripts locales / sensibles (no commitear)
reset-postgres-password.ps1
*.local.ps1
*.secret

# package-lock.json en raiz es huerfano (no hay package.json en raiz)
/package-lock.json

# Excel lock files
~$*.xlsx

# Backups locales (preprod testing)
backups/
*.dump


# Datos exportados (no versionar)
/outputs/
*.xlsx
*.xls
*.csv

# Grabaciones fuente de tutoriales (se sube el mp4 comprimido, no el mkv)
*.mkv

# ── Datos que no deben viajar en el repositorio ──────────────────────────────
# Facturas, audios, documentos de clientes y volcados de trabajo. Se anotan aqui
# porque ya pasaron una vez: habia audios de WhatsApp del personal commiteados.
*.zip
*.mkv
*.ogg
*.docx
*.xlsx
/*.pdf
/*.PDF
/*.sql
/*.py
/*.sh
/outputs/
/Claude/
*-handoff.md

# Cache de vitest. Lo genera `npm test` y no es de nadie: sin esto aparece como
# fichero sin seguimiento cada vez que se corre la suite.
.vite/
